import { Config } from '../../types.js';
import { Options } from '../../schema.js';
export declare function start(gridUrl: string | undefined, port: number, config: Config, options: Options): Promise<void>;
