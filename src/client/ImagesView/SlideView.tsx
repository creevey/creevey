import React from "react";
import { ViewProps } from "./ImagesView";

export class SlideView extends React.Component<ViewProps> {
  render() {
    const { url, actual, diff, expect } = this.props;
    return null;
  }
}
