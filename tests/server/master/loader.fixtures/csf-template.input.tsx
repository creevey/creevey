import React from 'react';

export default { title: 'Button' };

const Template = (args) => <button>Hello Button</button>;

export const Text = Template.bind({});
export const Button = Template.bind({});